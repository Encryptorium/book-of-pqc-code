[pytest]
# Use importlib mode so per-chapter test files with identical basenames
# (tests/chNN/test_vectors.py) do not collide in Python's default sys.modules
# import resolution. Each tests/chNN/ directory is independent and intentionally
# lacks __init__.py so chapters can be developed in isolation.
addopts = --import-mode=importlib
