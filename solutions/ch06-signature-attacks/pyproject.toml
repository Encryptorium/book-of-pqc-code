[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "signature-attacks"
version = "0.1.0"
description = "Chapter 6 of the Encryptorium Book of PQC: the textbook-RSA multiplicative forgery, the full-domain-hash repair, and ECDSA key recovery from a reused nonce."
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
